{%- comment -%}
  SUPERSEDED — renders nothing on purpose.

  The cookie consent banner is now sections/dressar-consent.liquid, a section in
  the footer group whose wording is editable in the theme editor. That is the one
  to change.

  The markup that used to live here read its text from `consent.*` translation
  keys that were never added to locales/en.default.json, so rendering it would
  have printed "Translation missing: en.consent.title" and so on to real
  customers. It is emptied rather than deleted because theme file deletion is
  blocked through the Admin API; delete it from Shopify admin if you want it gone.
{%- endcomment -%}
