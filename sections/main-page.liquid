<div class="wrap section">
  <div class="wrap--narrow" style="margin-inline:auto;padding-inline:0">
    <h1>{{ page.title }}</h1>
    <div class="rte">{{ page.content }}</div>
  </div>
</div>

{% schema %}
{ "name": "Page" }
{% endschema %}
